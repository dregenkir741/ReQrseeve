[project]
name = "reqrseeve"
version = "0.1.0"
description = "превращаю поток мыслей в оптимизированный план действий"
authors = [
    {name = "dregenkir",email = "dregenkir741@mail.ru"}
]
requires-python = ">=3.10,<4"
dependencies = [
    "pydantic (>=2.13.4,<3.0.0)",
    "sqlalchemy (>=2.0.49,<3.0.0)",
    "openai (>=2.36.0,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "gigachat (>=0.2.1,<0.3.0)",
    "httpx (>=0.28.1,<0.29.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "reqrseeve"
version = "0.1.0"
description = "..."
authors = ["..."]
package-mode = false